[es-git](../globals.md) / TreeIter

# Class: TreeIter

An iterator over the entries in a tree.

## Methods

### \[iterator\]()

> **\[iterator\]**(): `Iterator`\<[`TreeEntry`](TreeEntry.md), `void`, `void`\>

#### Returns

`Iterator`\<[`TreeEntry`](TreeEntry.md), `void`, `void`\>
